Binary campaign option — often paired with a long `hint` explaining the rules consequence.

```jsx
<Checkbox checked={blockByDefault} onChange={toggle}
  label="新建地形默认阻挡"
  hint="勾选后，新建的图形地形默认具备实体阻挡属性，防止棋子穿过" />
```
