Draggable piece on the 1ft tactical grid. Flat pigment fill by allegiance — no gradients, and the active-turn ring is steady rather than animated.

```jsx
<MapToken kind="PC" name="奥利奥 (战士)" active conditions={1} />
<MapToken kind="MONSTER" name="哥布林斥候 x3" size={40} />
```
